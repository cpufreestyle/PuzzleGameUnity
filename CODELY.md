

## Codely Structured Memories

### User

### Feedback

### Project
- [2026-09-06 20:09:57] [project] PuzzleGameUnity 仓库：本地是浅克隆（.git/shallow 截断于 5b98f59，无法直接 push 完整历史）；2026-09-06 起远端默认分支为 main 且历史被压成单提交 680d67f（内容=本地旧 3651d69），旧 origin/master 跟踪已废弃。**Why:** push 报 "remote unpack failed / did not receive expected object" 即浅克隆缺父对象所致。**How to apply:** 推送用 `git push origin main`（上游已指向 origin/main）；若再有历史重写，先 `git fetch origin main` 比对，把新提交 rebase --onto 远端提交后再推。
- [2026-09-06 21:57:58] [project] 本机访问 GitHub：HTTPS(443) 直连不可靠（git 报 "Error in the HTTP2 framing layer"/连接超时，api.github.com 403），SSH 通道（git@github.com）稳定可用。**Why:** 2026-09-06 克隆 minigame-tuanjie-transform-sdk 时 HTTPS 两次失败、SSH 一次成功。**How to apply:** git clone/添加 UPM 包一律用 SSH URL；UPM git 包也可先手动 SSH 克隆到 .codely-cli/extensions/ 再以 file: 引用（与 TJGenerators 同模式）。
- [2026-09-06 22:27:08] [project] 微信小游戏工具链（2026-09-06）：① 团结 `BuildTarget.WeixinMiniGame` 只产出 wasm player（index.html+.br），不含 game.json，转小程序必须用微信转换SDK（WXSDK，入口 `WXConvertCore.DoExport(true)`，产物在 DST/minigame，`WXExportError` 是 WXConvertCore 的嵌套枚举，配置资产路径硬编码为 `Assets/WX-WASM-SDK-V2/Editor/` 需预先建目录）；② 原版 wechat-miniprogram/minigame-unity-webgl-transform 已被 GitHub 下架，可用 fork 是 minigame-tuanjie-transform-sdk（已克隆到 .codely-cli/extensions/ 作源缓存）；③ SDK 的两个 asmdef 引用不存在的 `GUID:39e0a8d7...`/`Unity.InstantGame.Editor`，需删掉（有 #if UNITY_INSTANTGAME 保护，安全）；④ **关键坑：file: 引用（manifest 或 Packages/ 内嵌）在本机团结上会静默失败——包注册成功但内容永不导入、无任何报错，删缓存也无效；必须按官方手册把 SDK 直接放进 Assets/**（Editor+Runtime→Assets/WX-WASM-SDK，WebGLTemplates→Assets/WebGLTemplates）。**How to apply:** 批处理转换脚本 Assets/Editor/WeixinMinigameConvert.cs；AppID 占位 touristappid；输出 Builds/WeChatMiniGame/minigame；用户习惯把构建产物提交进仓库（Builds/ 已含 apk 和 wasm 输出）。
- [2026-09-06 23:06:39] [project] PuzzleGameUnity 微信小游戏构建：`"/Applications/Tuanjie/Hub/Editor/2022.3.62t12/Tuanjie.app/Contents/MacOS/Tuanjie" -batchmode -projectPath ... -executeMethod WeixinMiniGameBuild.Build -logFile <log>`，全程约 5-8 分钟（wasm 链接最久），产物固定落盘 Builds/WeChatMiniGame/{minigame,webgl}（locationPathName 会被管线规范化，勿直接手改 project.config.json 的 appid，会被下次构建覆盖——AppID 配置点在 WeixinMinigameConvert.cs 的 config.ProjectConf.Appid，现为 touristappid）。**Why:** 2026-09-06 实测；输出目录名与脚本参数不一致曾误判产物丢失。**How to apply:** 让用户用微信开发者工具打开 Builds/WeChatMiniGame/minigame；正式上传前换 AppID。
- [2026-09-07 18:57:52] [project] PuzzleGameUnity 微信小游戏：appid=wx27892251b37342a7（小游戏「邱明智慧拼图」，mp 后台在调试 Chrome 里保持登录态）；依赖的 Unity 适配插件 wxe5a48f1ed5f544b7 已添加、快适配已开启。**Why:** 2026-09-06/07 预览报 80082 permission deny 后逐一排查，最终确认插件早已授权，重启开发者工具后预览即成功。**How to apply:** 真机预览命令 `/Applications/wechatwebdevtools.app/Contents/MacOS/cli preview --project <minigame目录> --qr-format image --qr-output <工程内路径>`（二维码输出不能写 /tmp，沙盒会报"二维码输出路径无效"）；80082/黑屏时先重启开发者工具再试，别急着查配置。
- [2026-09-08 18:30:17] [project] PuzzleGameUnity 真机黑屏第二根因（2026-09-07）：Game.InitBoard 的 `currentImageIndex % puzzleImages.Length` 在空数组上崩溃（wasm 报 remainder by zero）。原因：场景 game.unity 的 Game 组件没有 puzzleImages 序列化（场景早于该字段）+ photo.jpg 不在 Resources 目录且 spriteMode=2（Multiple）导致 Resources.Load 取不到。修复：photo.jpg 复制到 Assets/Resources/（spriteMode 改 1）+ InitBoard 开头加空图集兜底（自动生成蓝底图）。另有 assetLoadType 0→1（资源分包本地加载）。**How to apply:** 类似 wasm 报 remainder/divide by zero 先查 % 和 / 的除数是否来自未初始化的序列化字段；小游戏转换的资源加载模式配置点在 WeixinMinigameConvert.cs 的 assetLoadType。

- [2026-09-13 00:40:18] [project] Unity 场景"僵尸对象"坑：编辑器 Play 态保存场景会把运行时 Instantiate 的对象固化进 .unity（本轮 16 个 piece-X-Y 全堆在原点、SpriteRenderer 引用悬空精灵，游戏脚本 InitBoard 只清自己 pieces 列表清不掉）。**Why:** 2026-09-12 排查真机布局错乱时发现，场景 YAML 67 个文档里 48 个是僵尸三件套（GO+SpriteRenderer+Transform）。**How to apply:** 清理用脚本按 `--- !u!` 文档边界解析、按 m_Name 正则匹配、级联删 m_Component 引用并过滤 SceneRoots 条目（.codely-cli/tmp/strip_zombie_pieces.py，改前必备份）；排查时先 `grep -c "m_Name: piece-"` 场景文件看有无烘焙残留。
- [2026-09-13 00:40:18] [project] TMP 中文字体（Static 子集）管线：TMP 3.0.6 的 `TryAddCharacters` 拒绝 Static 资产（报 "AtlasPopulationMode is set to Static"）→ 先 Dynamic 创建烘焙、再反射清 `m_SourceFontFile`、切回 Static（源 OTF 8.3MB 不入包）；1024² 图集只装 ~90 个 90pt 字形，超了 TryAddCharacters 整批失败图集全空；`AssetDatabase.ImportPackage` 在 -nographics 批处理下静默不落盘 → 手动解包 .unitypackage（gz+tar，按 pathname 恢复 .meta 保 GUID）；`TMP_Settings.instance` 缺失时会尝试打开 Importer 窗口导致批处理挂掉。**Why:** 2026-09-13 给拼图小游戏导中文字体（用户确认），三次批处理迭代踩齐。**How to apply:** 字体资产 Assets/Codely/Fonts/NotoSansSC-Regular SDF.asset（已挂 TMP_Settings.fallbackFontAssets）；UI 新增中文字符必须重跑子集烘焙（charset=运行时 UI 字符串全集，勿把代码注释里的中文烘进去），否则新字空白。
- [2026-09-13 00:40:18] [project] 微信开发者工具 CLI preview 两个坑：① preview 二维码是**生成时刻的代码快照**，重建后必须重新出码（本轮真机截图报旧 bug 的根因：用户扫了 22:32 旧码，而修复 23:20 才改入）；② preview 偶发 `Error: Timeout (code 10)` 卡死（等 5 分钟无果）→ 重启开发者工具（osascript quit + pkill 兜底，重开带 --remote-debugging-port=9223）后重试即愈。**How to apply:** 每次重建后先 `cli preview` 出新码再让用户扫；Timeout 先重启 DevTools 别急着查配置；CDP 探针下 Page.reload 会杀 webview 目标，要重建会话用 cli open 而非 reload。

### Reference

